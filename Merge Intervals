class Solution {
    public int[][] merge(int[][] intervals) {
        if (intervals.length <= 1) return intervals;

        // 1. Sort by the start time of each interval
        Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));

        List<int[]> result = new ArrayList<>();
        // 2. Initialize with the first interval
        int[] currentInterval = intervals[0];
        result.add(currentInterval);

        for (int[] nextInterval : intervals) {
            int currentEnd = currentInterval[1];
            int nextStart = nextInterval[0];
            int nextEnd = nextInterval[1];

            // 3. If they overlap, update the end of the current interval
            if (currentEnd >= nextStart) {
                currentInterval[1] = Math.max(currentEnd, nextEnd);
            } else {
                // 4. If no overlap, move to the next interval
                currentInterval = nextInterval;
                result.add(currentInterval);
            }
        }

        // 5. Convert list back to 2D array
        return result.toArray(new int[result.size()][]);
    }
}
