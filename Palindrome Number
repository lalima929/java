class Solution {
    public boolean isPalindrome(int x) {
        if(x<0) return false;
    int i,k,p=0;
    k=x;
    while(x!=0){
        i=x%10;
        p=p*10+i;
        x/=10;
    }
    
    if(k==p){
        return true;
    }
    else{
        return false;
    }
    }
}
